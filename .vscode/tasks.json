{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${workspaceRoot}/lexer.cpp",
                "${workspaceRoot}/util.cpp",
                "${workspaceRoot}/main.cpp",
                "-o",
                "${workspaceRoot}/main"
            ],
            "options": {
                "cwd": "/usr/bin"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}